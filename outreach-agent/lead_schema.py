"""Credential-free lead-sheet schema shared by production and local tooling."""

EXPECTED_HEADER = [
    "Name", "Email", "Company", "Status", "ThreadID", "SentAt", "EmailBody",
    "LeadReason", "EmailConfidence",
]
