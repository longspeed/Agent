# secrets
outreach-agent/.env
outreach-agent/credentials.json
outreach-agent/token.json
# per-developer MCP config -- copy .mcp.json.example and fill in your own
# Supabase project_ref; the real one isn't a secret (no key alongside it)
# but there's no reason to publish which project backs a given deployment
.mcp.json

# local data / generated artifacts
server.log
example-leads-sheet.csv
outreach-agent/reviews.db
outreach-agent/email_preview.txt
temporary screenshots/
.claude/scheduled_tasks.lock

# python
__pycache__/
*.pyc

# node
node_modules/
.gstack/

# throwaway QA/design screenshots from the landing-page builds
landing/shots/
site/shots/

# local-only tooling (separate repo, not part of this project)
CLIProxyAPI/

# app run logs (same category as server.log above)
agent-hub.log
agent-hub-run.log
agent-hub-run.err.log
smoke-uvicorn.*.log
uvicorn-*.log

# throwaway QA screenshots from the e2e pass (same as landing/shots, site/shots)
e2e-shots/

# locally-installed agent skills + their lockfile — tooling, not project code
.claude/skills/
skills-lock.json

# AI-agent tooling and internal working docs — kept locally, not published.
# TODOS.md and BUGS.md are the deliberate exception: those stay tracked.
.agents/
.claude/
claude.md
skill.md
Agent-Hub-*.md
Agent-Hub-*.pdf
