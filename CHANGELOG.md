# Changelog

## Unreleased

- Added a credential-free local demo queue at `/demo` with local OpenAPI docs.
- Added safe `--help` and `--version` handling to the worker and outreach CLIs.
- Added developer workflow smoke tests and removed hardcoded test-count claims
  from CI labels.
- Clarified manual review versus confirmed auto-send behavior in onboarding docs.
- Removed authenticated navigation controls from anonymous Getting Started.
